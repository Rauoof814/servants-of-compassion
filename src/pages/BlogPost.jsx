import useSite from '../store/useSite'; import { useParams } from 'react-router-dom';
export default function BlogPost(){ const {state}=useSite(); const {id}=useParams(); const post=(state.blog||[]).find(x=>x.id===id); if(!post) return <section className="section"><div className="container">Post not found.</div></section>; return(<section className="section"><div className="container grid gap-4"><div className="h2">{post.title}</div><div className="text-xs opacity-70">{new Date(post.date).toLocaleDateString()} • {post.author||'Team'}</div>{post.image&&<img src={post.image} className="rounded-xl border border-white/10"/>}<article className="prose prose-invert max-w-none" style={{whiteSpace:'pre-line'}}>{post.content}</article></div></section>); }