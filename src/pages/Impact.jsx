import useSite from '../store/useSite';
export default function Impact(){ const {state}=useSite(); return(<section className="section"><div className="container grid md:grid-cols-3 gap-4"><div className="card p-6"><div className="text-sm opacity-80">Ambulances funded</div><div className="h1 text-ukYellow">{state.stats.ambulances}</div></div><div className="card p-6"><div className="text-sm opacity-80">Medical kits delivered</div><div className="h1 text-ukYellow">{state.stats.kits.toLocaleString()}</div></div><div className="card p-6"><div className="text-sm opacity-80">Active volunteers</div><div className="h1 text-ukYellow">{state.stats.volunteers}</div></div></div><div className="container mt-8 grid md:grid-cols-3 gap-4">{(state.vehicles||[]).map(v=>(<div key={v.id} className="card p-4 grid gap-2"><div className="font-bold">{v.title}</div><div className="text-xs opacity-70">{new Date(v.date).toLocaleDateString()} — {v.region}</div>{v.photo&&<img src={v.photo} className="rounded-xl border border-white/10"/>}<div className="text-sm opacity-90">{v.status} • {v.notes}</div></div>))}</div></section>); }