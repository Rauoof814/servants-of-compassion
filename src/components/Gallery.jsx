import React from 'react'; export default function Gallery({images=[]}){ const [light,setLight]=React.useState(null); const next=(d)=>{if(light==null)return;const i=images.findIndex(x=>x===light);setLight(images[(i+d+images.length)%images.length]);}; return(<div><div className="grid grid-cols-2 md:grid-cols-4 gap-3">{images.map((src,i)=>(<img key={i} src={src} className="rounded-xl border border-white/10 cursor-zoom-in hover:opacity-90" onClick={()=>setLight(src)}/>))}</div>{light&&(<div className="fixed inset-0 bg-black/80 z-[120] p-6 flex items-center justify-center" onClick={()=>setLight(null)}><div className="relative w-[min(1200px,100%)]"><img src={light} className="rounded-2xl border border-white/20 w-full"/><div className="absolute inset-x-0 -bottom-12 flex justify-between pointer-events-none"><button className="btn pointer-events-auto" onClick={(e)=>{e.stopPropagation();next(-1)}}>◀</button><button className="btn pointer-events-auto" onClick={(e)=>{e.stopPropagation();next(+1)}}>▶</button></div></div></div>)}</div>); }